/**
 * React Native CLI config for drive-assistant-mobile.
 * Les dossiers ios/android natifs sont générés par le CLI React Native
 * (voir README) ; aucune plateforme native n'est requise pour le build JS
 * (npm run build:bundle).
 */
module.exports = {
  root: __dirname,
  reactNativePath: 'node_modules/react-native',
  dependencies: {},
};